<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.voice-input.client</string>

    <!-- 必须经 LaunchServices（open）拉起 App，辅助功能授权才能关联到
         VoiceInput.app；直接执行内部脚本会让授权失效（责任进程=launchd）。
         -W：等 App 退出，保持 launchd KeepAlive 语义 -->
    <key>ProgramArguments</key>
    <array>
        <string>/usr/bin/open</string>
        <string>-W</string>
        <string>-a</string>
        <string>@APP@</string>
    </array>

    <key>WorkingDirectory</key>
    <string>@ROOT@</string>

    <!-- 登录后自动启动；崩溃自动拉起（间隔≥10s） -->
    <key>RunAtLoad</key>
    <true/>
    <key>KeepAlive</key>
    <true/>
    <key>ThrottleInterval</key>
    <integer>10</integer>

    <key>StandardOutPath</key>
    <string>@HOME@/Library/Logs/voice-input.log</string>
    <key>StandardErrorPath</key>
    <string>@HOME@/Library/Logs/voice-input.log</string>
</dict>
</plist>
